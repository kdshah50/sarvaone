---
description: This repo is Sarvaone — not naranjogo3
alwaysApply: true
---

# Project identity

- **Product / workspace name: Sarvaone** (npm package `sarvaone`). US-first; English default UI, Spanish via `?lang=es` or the header toggle.
- **Production domain:** `https://sarvaone.com` (`NEXT_PUBLIC_APP_URL`).
- **Git:** The only remote should be `origin` → `https://github.com/kdshah50/Sarvaone.git`. Do not add or push to `naranjogo3` or other remotes unless the user explicitly asks.
- **Deploy / CI:** Next.js on **Vercel** (GitHub → Vercel project **sarvaone**). **Do not add or assume GitLab CI** unless the user explicitly asks.
- **Database:** Env must point to a **Sarvaone-dedicated Supabase project** — never the Mexico marketplace Supabase URL/keys unless the user explicitly says to share them.
